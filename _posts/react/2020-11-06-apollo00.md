---
layout: post
title: Apollo 소개
category: react
tags: [react, next]
comments: true
---

# Apollo 소개
> 노마드코더에서 강의를보며 정리했습니다. 틀린 내용이 있다면 편하게 댓글을 달아주세요!
``` 
npm i styled-components react-router-dom apollo-boost @apollo/react-hooks graphql
```

## Apollo는?

- Apollo 서버는 GraphQL API를 제공하는 서버를 개발할 수 있게 도와주는 패키지로서 기존에 Node.js에서 사용하는 Express와 역할이 비슷하다. 

## Apollo 구성하기

- apollo.js <br/>

> uri를 고정시켜도 되는 이유가 graphql는 uri에서 json을 받아오는 구조가 아닌, 쿼리문을 통해 데이터를 받아오는 형태이기 때문이다.


```javascript
import ApolloClient from 'apollo-boost';

const client = new ApolloClient({
  uri: 'https://movieql.now.sh/',
});

export default client;

```


- index.js

```javascript
import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';
import { ApolloProvider } from '@apollo/react-hooks';
import client from './apollo';

ReactDOM.render(
  <ApolloProvider client={client}>
    <App />
  </ApolloProvider>,
  document.getElementById('root')
);
```